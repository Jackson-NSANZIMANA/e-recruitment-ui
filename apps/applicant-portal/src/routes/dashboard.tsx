/**
 * Dashboard for authenticated applicants — shows their active applications
 * and quick links to apply or check status.  Delegates rendering to the
 * StatusPage which already contains the full application-list logic.
 */
export { default } from "./status.js";
