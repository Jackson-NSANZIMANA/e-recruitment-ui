{
  "service": "biometric-service",
  "cases": [
    { "id": "evaluated-verified", "schema": "BiometricEvaluated", "expect": "accept",
      "why": "verify-biometric EVALUATED branch, full breakdown.",
      "value": { "status": "EVALUATED", "verified": true, "sessionId": "a3f1c2d4-5b6e-4f70-8192-0a1b2c3d4e5f", "applicantId": "b4e2d3c5-6f71-4a82-93a4-1b2c3d4e5f60", "livenessPass": true, "faceMatchPass": true } },
    { "id": "evaluated-rejected-is-200", "schema": "BiometricEvaluated", "expect": "accept",
      "why": "A FAILED MATCH IS A 200. An assertOk-shaped client reads a rejected impostor as a successful check-in.",
      "value": { "status": "EVALUATED", "verified": false, "sessionId": "a3f1c2d4-5b6e-4f70-8192-0a1b2c3d4e5f", "applicantId": "b4e2d3c5-6f71-4a82-93a4-1b2c3d4e5f60", "livenessPass": true, "faceMatchPass": false } },
    { "id": "rejects-capture-ref-echo", "schema": "BiometricEvaluated", "expect": "reject",
      "why": "The raw capture reference is never echoed.",
      "value": { "status": "EVALUATED", "verified": true, "sessionId": "a3f1c2d4-5b6e-4f70-8192-0a1b2c3d4e5f", "applicantId": "b4e2d3c5-6f71-4a82-93a4-1b2c3d4e5f60", "livenessPass": true, "faceMatchPass": true, "captureRef": "s3://captures/x.jpg" } },
    { "id": "403-agency-mismatch", "schema": "BiometricForbidden403", "expect": "accept",
      "why": "UNIQUE IN THE PLATFORM: a 403 keyed on status, not error.",
      "value": { "status": "AGENCY_MISMATCH" } },
    { "id": "403-withauth-shape", "schema": "BiometricForbidden403", "expect": "accept",
      "why": "The SAME status also arrives as withAuth's { error, detail }. Two members with NO shared property, which is why this union is untagged.",
      "value": { "error": "FORBIDDEN", "detail": "Officer principal required." } },
    { "id": "403-rejects-merged-shape", "schema": "BiometricForbidden403", "expect": "reject",
      "why": "Nobody emits both keys. A client that 'helpfully' normalises the two shapes into one would produce this, and it corresponds to no real response.",
      "value": { "error": "FORBIDDEN", "status": "AGENCY_MISMATCH" } },
    { "id": "invalid-invitation-bare", "schema": "InvalidInvitation422", "expect": "accept",
      "why": "Bare on purpose: distinguishing forged from expired at the venue door teaches a forger which half worked.",
      "value": { "status": "INVALID_INVITATION" } },
    { "id": "invalid-invitation-rejects-reason", "schema": "InvalidInvitation422", "expect": "reject",
      "why": "The guard on that decision.",
      "value": { "status": "INVALID_INVITATION", "reason": "SIGNATURE_VALID_BUT_EXPIRED" } },
    { "id": "verify-request", "schema": "VerifyBiometricRequest", "expect": "accept",
      "why": "Two fields. The officer's agency comes from the token.",
      "value": { "qrSignedToken": "QR.eyJhbGciOiJFZDI1NTE5In0.sig", "captureRef": "capture-01" } },
    { "id": "verify-request-rejects-agency", "schema": "VerifyBiometricRequest", "expect": "reject",
      "why": "AN AGENCY FIELD HERE WOULD BE THE CROSS-AGENCY BYPASS THIS DESIGN EXISTS TO PREVENT. It comes from the verified token, never the body.",
      "value": { "qrSignedToken": "QR.sig", "captureRef": "capture-01", "agency": "RDF" } }
  ]
}
