{
  "name": "@usrp/shared-types",
  "version": "1.0.0",
  "private": true,
  "description": "DEPRECATED — quarantined and frozen. Use @usrp/contracts. Kept only until @usrp/api-client, @usrp/ui, @usrp/officer-console and @usrp/applicant-portal have migrated off it.",
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "main": "./src/index.ts",
  "scripts": {
    "lint": "node --experimental-strip-types ../contracts/scripts/lint/cli.ts --package shared-types",
    "test": "node --experimental-strip-types --import ../contracts/test/register.mjs --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "~5.4.0"
  }
}
