{
  "name": "@usrp/feature-scheduling",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Slot invitation key, signed QR invitations, offline verification.",
  "license": "UNLICENSED",
  "exports": { ".": "./src/index.ts", "./locales/*": "./locales/*" },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:model": "node --experimental-strip-types --test src/model/*.test.ts"
  },
  "//peerDependencies": "React, ReactDOM and the router are PEERS, not dependencies. The host app owns the single instance; a second React in the graph throws 'invalid hook call' and resolves context to the wrong provider. The devDependency copies exist only so this slice typechecks and tests standalone.",
  "dependencies": {
    "@atlaskit/css": "^1.0.0",
    "@atlaskit/primitives": "^22.2.1",
    "@atlaskit/section-message": "^10.0.0",
    "@compiled/react": "^1.0.0",
    "@usrp/contracts": "workspace:*",
    "@usrp/i18n": "workspace:*"
  },
  "peerDependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.0"
  },
  "devDependencies": {
    "@atlaskit/eslint-plugin-design-system": "^16.10.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@usrp/eslint-config": "workspace:*",
    "eslint": "^9.9.0",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "react-router-dom": "6.30.1",
    "typescript": "~5.4.0",
    "vitest": "^2.1.0"
  }
}
