import React from 'react';import SectionMessage from '@atlaskit/section-message';import {Text} from '@atlaskit/primitives/compiled';import {useTranslation} from '@usrp/i18n';import {slotMessageKey,slotVisibility} from '../model/invitation.ts';export function SlotNotice({status}:{readonly status:string}):React.ReactElement|null{const {t}=useTranslation('scheduling');const v=slotVisibility(status);return v==='NOT_ASSIGNED'?null:<SectionMessage appearance="information"><Text as="p">{t(slotMessageKey(v))}</Text></SectionMessage>}
