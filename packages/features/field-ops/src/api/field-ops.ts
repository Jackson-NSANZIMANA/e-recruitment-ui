import type {BiometricEvaluated} from '../model/biometric.ts';import type {SyncScoresOk} from '../model/sync-result.ts';import type {SliceTransport} from './transport.ts';export const registerWalkIn=(t:SliceTransport,b:Readonly<Record<string,unknown>>):Promise<unknown>=>t.call('registerWalkIn',{body:b});export const vetWalkIn=(t:SliceTransport,b:Readonly<Record<string,unknown>>):Promise<unknown>=>t.call('vetWalkIn',{body:b});export const verifyBiometric=(t:SliceTransport,qrSignedToken:string,captureRef:string):Promise<BiometricEvaluated>=>t.call('verifyBiometric',{body:{qrSignedToken,captureRef}});export const enrollDevice=(t:SliceTransport,deviceId:string,publicKeyPem:string):Promise<unknown>=>t.call('enrollFieldDevice',{body:{deviceId,publicKeyPem}});export const syncScores=(t:SliceTransport,records:readonly unknown[]):Promise<SyncScoresOk>=>t.call('syncFieldScores',{body:{records}});export const resolveConflict=(t:SliceTransport,applicationId:string,scoreId:string,resolution:string):Promise<unknown>=>t.call('resolveFieldConflict',{body:{applicationId,scoreId,resolution}});
