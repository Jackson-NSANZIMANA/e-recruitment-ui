import React,{lazy} from 'react';import type {RouteObject} from 'react-router-dom';const WalkIn=lazy(()=>import('./walk-in.tsx'));const CheckIn=lazy(()=>import('./check-in.tsx'));const Conflicts=lazy(()=>import('./conflicts.tsx'));export const FieldOpsRoutes:readonly RouteObject[]=[{path:'field/walk-in',element:<WalkIn/>},{path:'field/check-in',element:<CheckIn/>},{path:'field/conflicts',element:<Conflicts/>}];
