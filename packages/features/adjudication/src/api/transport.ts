export interface CallOptions{readonly body?:unknown;readonly query?:Readonly<Record<string,string>>;readonly signal?:AbortSignal}export interface SliceTransport{call<T>(operationId:string,options?:CallOptions):Promise<T>}export const ADJUDICATION_OPERATIONS=['listAmberQueue','acceptApplication','adjudicateApplication','recordMedicalReview','recordFinalDecision','findApplicationById'] as const;export type AdjudicationOperation=(typeof ADJUDICATION_OPERATIONS)[number];
