import type {AmberRow} from '../model/amber.ts';import {TRANSITION_OPERATION,type Transition} from '../model/transitions.ts';import type {CallOptions,SliceTransport} from './transport.ts';export interface AmberQueueOk{readonly agency:string;readonly queue:readonly AmberRow[]}export const listAmberQueue=(t:SliceTransport,o?:CallOptions):Promise<AmberQueueOk>=>t.call('listAmberQueue',o);export const applyTransition=(t:SliceTransport,x:Transition,body:Readonly<Record<string,unknown>>):Promise<unknown>=>t.call(TRANSITION_OPERATION[x],{body});
