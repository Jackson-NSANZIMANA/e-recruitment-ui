import type {ErasureQueueRow} from '../model/erasure.ts';import type {WithdrawOutcome} from '../model/self-withdrawal.ts';import type {CallOptions,SliceTransport} from './transport.ts';export interface ErasureQueueOk{readonly requests:readonly ErasureQueueRow[]}export const listErasureRequests=(t:SliceTransport,o?:CallOptions):Promise<ErasureQueueOk>=>t.call('listErasureRequests',o);export const declineErasureRequest=(t:SliceTransport,requestId:string,note:string):Promise<{readonly status:'DECLINED'}>=>t.call('declineErasureRequest',{body:{requestId,note}});export const fileMyErasureRequest=(t:SliceTransport):Promise<unknown>=>t.call('fileMyErasureRequest');export const getMyErasureRequest=(t:SliceTransport):Promise<unknown>=>t.call('getMyErasureRequest');export const withdrawMyApplication=(t:SliceTransport,applicationId:string):Promise<WithdrawOutcome>=>t.call('withdrawMyApplication',{body:{applicationId}});
