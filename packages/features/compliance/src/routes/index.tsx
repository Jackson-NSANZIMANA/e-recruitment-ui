import React,{lazy} from 'react';import type {RouteObject} from 'react-router-dom';const Queue=lazy(()=>import('./dpo-queue.tsx'));const Withdraw=lazy(()=>import('./withdraw.tsx'));export const ComplianceOfficerRoutes:readonly RouteObject[]=[{path:'compliance/erasure-queue',element:<Queue/>}];export const ComplianceCitizenRoutes:readonly RouteObject[]=[{path:'my/withdraw',element:<Withdraw/>}];
