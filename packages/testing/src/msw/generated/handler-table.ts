export interface MockOutcome{readonly operationId:string;readonly scenario:string;readonly service:string;readonly method:string;readonly path:string;readonly status:number;readonly fixture:string|null;readonly body:unknown}export const MOCK_OUTCOMES:readonly MockOutcome[]=[];export const DEFAULT_OUTCOMES:readonly MockOutcome[]=MOCK_OUTCOMES.filter(x=>x.scenario==='success');export function outcomeFor(operationId:string,scenario='success'):MockOutcome{const x=MOCK_OUTCOMES.find(y=>y.operationId===operationId&&y.scenario===scenario);if(!x)throw new Error(`No generated mock for ${operationId}/${scenario}`);return x}}
