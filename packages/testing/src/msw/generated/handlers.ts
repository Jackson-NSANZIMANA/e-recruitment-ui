import {http,HttpResponse} from 'msw';import {DEFAULT_OUTCOMES,MOCK_OUTCOMES,type MockOutcome} from './handler-table.ts';const method=(v:string)=>(v==='GET'?http.get:http.post);const toHandler=(o:MockOutcome)=>method(o.method)(`*${o.path}`,()=>o.body===null?new HttpResponse(null,{status:o.status}):HttpResponse.json(o.body as Record<string,unknown>,{status:o.status}));export const handlers=DEFAULT_OUTCOMES.map(toHandler);export function scenario(operationId:string,name:string){const o=MOCK_OUTCOMES.find(x=>x.operationId===operationId&&x.scenario===name);if(!o)throw new Error(`No generated scenario ${operationId}/${name}`);return toHandler(o)}
