import React,{Suspense,type ReactElement} from 'react';import {render,type RenderResult} from '@testing-library/react';import {MemoryRouter} from 'react-router-dom';import {axe} from 'jest-axe';export const renderSlice=(ui:ReactElement,route='/'):RenderResult=>render(<MemoryRouter initialEntries={[route]}><Suspense fallback={null}>{ui}</Suspense></MemoryRouter>);export async function expectAccessible(container:HTMLElement):Promise<void>{const results=await axe(container);(globalThis as any).expect(results).toHaveNoViolations()}
